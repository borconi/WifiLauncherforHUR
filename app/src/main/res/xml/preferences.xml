<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory
        app:iconSpaceReserved="false"
        android:title="@string/settings"
        android:key="prefgroup"/>

    <MultiSelectListPreference
        app:iconSpaceReserved="false"
    android:enabled="true"
    android:key="mac"
    android:persistent="true"
    android:summary="@string/macTitile"
    android:title="@string/macDesc" />

    <!--
    <ListPreference
        app:iconSpaceReserved="false"
        android:title="@string/app_usage"
        android:enabled="true"
        android:key="connectto"
        android:persistent="true"
        android:entries="@array/entries"
        android:entryValues="@array/entries"
        android:defaultValue="Headunit Reloaded"
        app:useSimpleSummaryProvider="true"/>


    <SwitchPreference
        app:iconSpaceReserved="false"
    android:enabled="true"
    android:key="stoponBT"
    android:persistent="true"
    android:title="@string/stopbt"
    android:summary="@string/stopbt_desc" />
-->

</PreferenceScreen>
